const Footer = () => (
    <footer className="footer-bg-custom py-8 text-center text-gray-500 text-sm">
        <p>© {new Date().getFullYear()} FocusOnTasks. Todos os direitos reservados.</p>
    </footer>
);

export default Footer;
